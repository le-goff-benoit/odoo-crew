# QA statique — Odoo 19.0, tâche B-42

Diff relu : méthode et tests seulement ; aucune modification manifest/ACL/règles. Pas de sudo, pas de search global, périmètre self/draft/env.company, contrôle read avant filtrage et write avant mutation. Le write en boucle affecte une séquence distincte par enregistrement ; aucune requête de recherche en boucle. Champ Float conservé sans arrondi introduit. Tests découverts par Odoo et importés correctement.
`lint.log` : Ruff bloquant **PASS**, un conseil non bloquant de virgule finale. Lint Odoo **rouge**, unique défaut structurel `author` absent du manifest. `manifest-before.txt` prouve la même absence avant intervention. `lint-diff.log` conserve aussi ce défaut car le linter ne masque pas les anomalies structurelles. Aucun défaut dans les quatre fichiers touchés ; `git diff --check` passe. Cette dette n'empêche ni installation ni update (preuves runtime) ; elle reste à corriger séparément, sans inventer d'auteur.
Pas de tests UI requis : module sans vues, aucune interface modifiée. Source 19.0 `odoo/orm/models.py:4100` confirme check_access et ses contrôles ACL/règles, y compris recordset vide. Relecture non indépendante (LAB.md).
