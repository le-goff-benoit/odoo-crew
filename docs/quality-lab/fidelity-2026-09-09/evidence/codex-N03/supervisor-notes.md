Le superviseur a rendu le code 0 après oracle et nettoyage. Sa sortie de terminal contient deux traces `ConnectionRefusedError` du proxy ; elles sont conservées comme observation, sans les transformer en résultats métier. Le lancement attend l'authentification RPC avant de déclarer la base prête ; les appels du parcours et l'oracle ont ensuite leurs preuves de succès. Aucune nouvelle exécution n'a été faite pour supprimer ces traces.

Les fichiers du projet sont archivés sans .git ni __pycache__. Le log de versionnement atteste le commit local. Les modes Unix originaux sont dans project-modes.json : Git ne conserve pas les bits d'écriture de groupe. Pour rejouer uniquement un contrôle de fraîcheur, restaurer ces modes dans une copie jetable montée à /work, racine historique des preuves ; ne pas relancer le flow ni réclamer une base encore disponible.
